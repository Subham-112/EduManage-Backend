export enum StudentStaus {
    ACTIVE = "active",
    INACTIVE = "inactive",
    SUSPENDED = "suspended",
}

export enum TenantStatus {
    REVIEW = "review",
    VERIFIED = "verified",
    ACTIVE = "active",
    INACTIVE = "inactive",
    SUSPENDED = "suspended",
}

export enum OwnerStatus {
    ACTIVE = "active",
    INACTIVE = "inactive",
    SUSPENDED = "suspended",
}

export enum TeacherStatus {
    ACTIVE = "active",
    INACTIVE = "inactive",
    SUSPENDED = "suspended",
}

export enum BranchStatus {
    REVIEW = "review",
    VERIFIED = "verified",
    ACTIVE = "active",
    INACTIVE = "inactive",
    SUSPENDED = "suspended",
}